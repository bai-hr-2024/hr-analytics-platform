# 🚀 人力资源分析平台 · 稳定部署指南

> 目的：解决原发布链接（app.workbuddy.link）**频繁 502、极其不稳定** 的问题。
> 根因是免费容器在空闲/流量波动时会休眠甚至被回收，且静态资源与数据卷不可靠。
> 本指南提供 **Railway（首选）、Render、通用 Docker** 三种方案，实现团队多人访问 + 数据持久化。

---

## 一、为什么改用 Railway？

| 对比项 | 原 workbuddy.link 免费容器 | Railway / Render 容器 |
|---|---|---|
| 稳定性 | 频繁 502、易被回收 | 常驻运行 + 自动重启，稳定 |
| 数据持久化 | 重建后数据易丢 | 挂载持久卷（Volume/Disk） |
| 多人访问 | 只有一个不稳定公网域名 | 每服务独立稳定公网域名 |
| 静态资源 | 间歇性 502 | 由同一进程统一托管，稳定 |

**本项目已内置好一切**：Dockerfile、数据目录可配置（`DATA_DIR` 环境变量）、`/api/health` 健康检查、JSON 文件持久化到 `/app/data`。

---

## 二、方案 A：Railway 部署（首选 ⭐）

Railway 上手最快，且官方提供每月免费额度（约 500 小时/月，足够一个常驻服务）。

### 前提
- 一个 GitHub 账号（用来托管本项目代码）
- 一个 Railway 账号（可用 GitHub 直接登录）

### 第 1 步：把本项目推到 GitHub

在沙箱/本地终端执行：

```bash
cd /workspace
git init
git add .
git commit -m "HR analytics platform + Docker deploy config"
# 先在 GitHub 上新建一个空仓库，再推送
git remote add origin https://github.com/<你的用户名>/hr-analytics-platform.git
git push -u origin main
```

> 已包含 `.dockerignore`，`node_modules`、`data`、日志都不会被推上去。

### 第 2 步：Railway 导入仓库

1. 打开 <https://railway.app>，用 GitHub 登录。
2. 点 **New Project → Deploy from GitHub repo**。
3. 选择 `hr-analytics-platform` 仓库。
4. Railway 会自动读取根目录的 `Dockerfile`（无需额外配置），开始构建并部署。
5. 等状态变绿后，点击服务顶部域名即可访问（域名形如 `xxxxx.up.railway.app`）。

### 第 3 步：开启数据持久卷（重要！）

数据默认写在容器内 `/app/data`，**容器重建会丢数据**。务必挂载持久卷：

1. 进入该服务的 **Settings → Volumes**。
2. **New Volume**，挂载路径填：`/app/data`
3. 容量选 **1 GB**（免费额度内）。
4. 保存后 Railway 会自动重启服务，之后 JSON 数据就存在持久卷里，重建不丢。

### 第 4 步：绑定自定义域名（可选）

**Settings → Networking → Custom Domain**，填入你的域名并按其提示配置 CNAME 即可。

### 第 5 步：AI 大模型配置
打开页面右上角 **AI 设置**，填写你的 DeepSeek/通义/Kimi 等 API Key（保存在服务端 data 卷中），保存后即可使用"一句话识别 + 任务把控分析"。

---

## 三、方案 B：Render 部署（备选）

Render 同样稳定、有免费额度，支持本仓库的 `render.yaml` 蓝图一键识别。

1. <https://render.com> 用 GitHub 登录。
2. **New → Blueprint**，选择本仓库（会自动读取 `render.yaml`）。
3. 打开渲染后的服务，其会：
   - 用 Docker 构建（`./Dockerfile`）
   - 挂载 1GB 持久盘到 `/app/data`
   - 配置健康检查 `/api/health`
4. 把 `render.yaml` 里的 `repo` 地址改成你的真实仓库后推送即可。

> 免费套餐在 15 分钟无流量后会休眠，首次访问会慢几秒（自动唤醒），但不影响数据。

---

## 四、方案 C：任意 Docker 主机（最可控）

如果你有自己的服务器（阿里云/腾讯云/VPS），用 Docker 部署最可控：

```bash
# 1. 构建镜像
docker build -t hr-analytics-platform .

# 2. 建数据目录并运行（挂载卷持久化）
mkdir -p /srv/hr-data
docker run -d --name hr-app \
  -p 8080:3000 \
  -e PORT=3000 \
  -e DATA_DIR=/app/data \
  -v /srv/hr-data:/app/data \
  --restart unless-stopped \
  hr-analytics-platform

# 3. 验证
curl http://localhost:8080/api/health
# 返回 {"ok":true,"count":...,"tasks":...} 即成功
```

之后用 Nginx/Caddy 反代到 8080，即可加 HTTPS 与域名。

**升级代码**：`git pull` 后重跑 `docker build` + `docker run`（数据都在 `/srv/hr-data`，不丢）。

---

## 五、本地验证（推荐先跑通）

在沙箱里直接验证镜像正确性：

```bash
# 构建
docker build -t hr-analytics-platform .

# 运行（指定端口 + 数据卷）
docker run -d --name hr-test -p 3000:3000 \
  -e DATA_DIR=/app/data -v $(pwd)/data:/app/data \
  hr-analytics-platform

# 健康检查
curl http://localhost:3000/api/health
```

能看到 `{"ok":true}` 且页面正常打开，说明镜像没问题，可放心推平台。

---

## 六、关键配置速查

| 环境变量 | 说明 | 默认 |
|---|---|---|
| `PORT` | HTTP 端口 | `3000` |
| `DATA_DIR` | JSON 数据存储目录（务必挂卷） | `/app/data`（容器内） |
| `ENABLE_AI` 等 | LLM 配置在页面「AI 设置」中填写，存于 `DATA_DIR` | — |

### 文件结构（Docker 相关）
```
├── Dockerfile        # 生产镜像（node:22-alpine，含健康检查）
├── .dockerignore     # 排除 node_modules/data/日志 等
├── railway.json      # Railway 构建声明
├── render.yaml       # Render 蓝图（含持久盘）
├── package.json      # 依赖（仅 express）
└── server.js         # Express 服务 + JSON 持久化
```

---

## 七、常见问题

**Q1：域名能访问但偶尔 502？**
免费平台在空闲期会休眠，唤醒需几秒属正常。若频繁 502，改用 Railway 常驻 + 确认健康检查路径 `/api/health`。

**Q2：数据会丢吗？**
只要挂了持久卷（Railway Volume / Render Disk / 自己主机的 `-v`）就不会丢。忘记挂载 + 重建容器才会丢。

**Q3：多人同时编辑会冲突吗？**
当前为 JSON 文件存储，适合几十人以内的小团队日常记录。若人数较多或并发写入高，可后续把存储层换为 SQLite/Postgres，接口契约不变，无需改前端。

---

部署完成后，把生成的公网域名分享给团队即可多人同时访问，数据集中保存、长期稳定。
